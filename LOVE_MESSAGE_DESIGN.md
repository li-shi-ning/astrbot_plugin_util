# 随机群友土味情话

在指定群聊中随机选择一名有效群成员，提及对方并发送一条带“喵”语气的土味情话。

## 功能边界

- 仅支持 AIOCQHTTP 群聊。
- 仅在配置列表中的群聊生效。
- 不提供全局配置。
- 每个群聊独立开关。
- 首版仅支持群内命令触发，不支持定时触发和随机概率触发。
- 每次触发只选择一名群成员、发送一条情话。

## 触发命令

`/土味情话`

处理流程：

1. 验证事件来自群聊。
2. 按群号查找配置。
3. 检查该群功能开关和冷却状态。
4. 调用 `await event.get_group()` 获取当前群资料和成员。
5. 排除无效候选成员。
6. 随机选择一名候选成员。
7. 从该群配置的情话列表随机选择一条。
8. 提及目标成员并发送情话。
9. 记录该群最后触发时间和当日触发次数。

## 分群配置

配置根项：`group_love_message_rules`

类型：`template_list`

每条规则：

| 字段 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `group_id` | `string` | `""` | QQ 群号。 |
| `enabled` | `bool` | `true` | 本群独立开关。 |
| `love_messages` | `list[string]` | `[]` | 本群土味情话词库。 |
| `cooldown_seconds` | `int` | `300` | 本群两次成功触发之间的最短间隔。 |
| `daily_limit` | `int` | `10` | 本群每日成功触发上限；`0` 表示不限制。 |
| `exclude_sender` | `bool` | `true` | 是否排除命令发送者。 |
| `exclude_admins` | `bool` | `false` | 是否排除群主和管理员。 |

同一个群号只允许配置一条规则。重复群号时使用第一条有效配置，并记录警告。

## 情话模板

情话可以包含以下变量：

| 变量 | 内容 |
| --- | --- |
| `{name}` | 目标成员群昵称；为空时使用 QQ 号。 |
| `{user_id}` | 目标成员 QQ 号。 |

配置示例：

```text
遇见{name}以后，我的心就像奶茶里的珍珠，怎么都沉不下去喵。
{name}，你知道我的缺点是什么吗？是缺点你喵。
别人问我喜欢什么样的人，我就开始描述{user_id}喵。
```

如果模板格式错误，跳过该条模板并尝试其他可用模板。所有模板都无效时不发送。

## 候选成员过滤

必须排除：

- 机器人自身。
- QQ 号为空的成员。
- OneBot 返回的匿名或无效成员记录。

按配置排除：

- 命令发送者。
- 群主和管理员。

不排除普通机器人账号，因为 OneBot 群成员数据不保证提供可靠的机器人标识。

候选列表为空时返回：

```text
没有找到可以说情话的群友喵。
```

## 输出格式

消息链：

1. `Comp.At(qq=目标 QQ 号)`
2. `Comp.Plain(" " + 格式化后的情话)`

示例：

```text
@群友 你知道我的缺点是什么吗？是缺点你喵。
```

情话配置未以“喵”“喵~”或“喵。”结尾时，插件自动追加“喵”。

## 冷却与次数限制

- 状态按群号隔离。
- 只有成功发送情话后才更新冷却时间和每日次数。
- 冷却期间触发时返回剩余秒数。
- 达到每日上限时返回当日已达上限。
- 日期按 AstrBot 运行环境的本地日期计算。
- 插件重载或 AstrBot 重启后，内存状态清空。

首版不持久化冷却和每日次数。

## 异常处理

| 场景 | 行为 |
| --- | --- |
| 私聊触发 | 返回“该命令只能在群聊中使用”。 |
| 群未配置 | 不响应。 |
| 本群开关关闭 | 不响应。 |
| 情话列表为空 | 记录警告，不发送。 |
| 获取群成员失败 | 记录异常，返回“暂时找不到群友喵”。 |
| 候选成员为空 | 返回“没有找到可以说情话的群友喵”。 |
| 冷却中 | 返回剩余冷却时间。 |
| 达到每日上限 | 返回当日已达上限。 |

## 模块设计

### `core/love_message.py`

- `GroupLoveMessageRule`: 单群规则。
- `load_group_love_message_rules(config)`: 加载分群配置。
- `filter_candidates(members, ...)`: 过滤候选成员。
- `choose_member(candidates, rng)`: 随机选择成员。
- `choose_message(messages, rng)`: 随机选择有效模板。
- `format_message(template, member)`: 替换模板变量并补充“喵”。
- `LoveMessageRateLimiter`: 管理分群冷却和每日次数。

### `main.py`

- 注册 `/土味情话` 命令。
- 调用 `await event.get_group()`；AIOCQHTTP 适配器内部通过 OneBot `get_group_info` 和 `get_group_member_list` 获取数据。
- 组装 `Comp.At` 与 `Comp.Plain` 消息链。
- 记录日志和处理平台异常。

## 测试清单

- [ ] 未配置群聊不响应。
- [ ] 关闭开关的群聊不响应。
- [ ] 私聊返回群聊限制提示。
- [ ] 正常获取成员并随机选择一人。
- [ ] 排除机器人自身。
- [ ] 按配置排除命令发送者。
- [ ] 按配置排除群主和管理员。
- [ ] 空候选列表返回提示。
- [ ] 群成员接口失败返回提示。
- [ ] 空情话列表不发送。
- [ ] 模板变量正确替换。
- [ ] 情话自动补充“喵”。
- [ ] 冷却状态按群隔离。
- [ ] 每日次数按群隔离。
- [ ] 冷却或次数限制失败时不增加计数。
- [ ] 成功发送后更新冷却和次数。

## 验收标准

- 配置群内执行 `/土味情话` 后，只从当前群有效成员中选择目标。
- 不选择机器人自身，不跨群选择成员。
- 输出包含目标成员提及和一条本群配置的情话。
- 每群开关、词库、冷却和每日上限互不影响。
- 接口异常、空成员、空词库和无效模板均不会导致插件崩溃。
