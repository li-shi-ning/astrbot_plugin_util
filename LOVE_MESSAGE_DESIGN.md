# 土味情话指令

通过指令确定目标用户，提及目标并从内置词库随机发送一条带“喵”语气的情话。

## 指令

```text
/土味情话
/土味情话 123456789
/土味情话 @群友
```

支持 AIOCQHTTP 群聊和私聊，不需要配置开关。

## 目标解析

与 `astrbot_plugin_NullDox` 的 `/盒 [QQ号|@用户]` 指令一致：

1. 有效的显式 QQ 参数。
2. 消息中最后一个非机器人 `@`。
3. 消息中最后一个机器人自身 `@`。
4. 命令发送者。

无显式目标或未检测到有效 `@` 时，默认目标为命令发送者。

以下目标会被忽略：

- `@全体成员`。
- 空 QQ。
- 非纯数字 QQ。

## 情话词库

文件：`core/love_messages.txt`

- 每行一条情话。
- 空行自动忽略。
- 指令触发时随机选择一条。
- `docs/情话.md` 不参与运行，也不加入插件包。

情话未以“喵”结尾时自动追加“喵”。

## 输出

消息链：

1. `Comp.At(qq=目标 QQ)`
2. `Comp.Plain(" " + 随机情话)`

示例：

```text
@群友 不要抱怨，抱我。喵
```

## 异常处理

| 场景 | 行为 |
| --- | --- |
| 词库文件不存在或为空 | 记录警告，不发送。 |
| 显式 QQ 非法 | 继续检测 `@`，没有有效 `@` 时回退发送者。 |
| 无有效目标且发送者 QQ 无效 | 返回“未找到有效目标喵。”。 |

## 测试

- [x] 加载词库并忽略空行。
- [x] 显式 QQ 优先。
- [x] 使用最后一个非机器人 `@`。
- [x] 无目标时回退发送者。
- [x] 非法显式 QQ 回退发送者。
- [x] 情话自动追加“喵”。
