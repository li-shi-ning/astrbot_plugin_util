{
  "group_keyword_voices": {
    "type": "template_list",
    "description": "分群关键词语音",
    "hint": "只检测列表中配置的群聊，不提供全局配置。同一群号可以添加多条规则，分别配置关键词和语音目录。",
    "default": [],
    "templates": {
      "group_keyword_voice": {
        "name": "群关键词语音",
        "hint": "为一个群配置关键词和存放语音文件的本地目录。",
        "display_item": "group_id",
        "hide_hint_in_list": true,
        "items": {
          "group_id": {
            "type": "string",
            "description": "群号",
            "hint": "填写 QQ 群号，例如 123456789。",
            "default": ""
          },
          "enabled": {
            "type": "bool",
            "description": "启用本群关键词语音",
            "hint": "关闭后保留本群配置，但不再检测和回复。",
            "default": true
          },
          "keywords": {
            "type": "list",
            "description": "触发关键词",
            "hint": "群消息包含任一关键词时触发；英文匹配不区分大小写。",
            "default": [],
            "items": {
              "type": "string"
            }
          },
          "audio_directory": {
            "type": "string",
            "description": "语音目录",
            "hint": "填写一个本地目录路径。插件按文件名排序，发送目录中的第一个 mp3、wav、amr、silk、ogg 或 m4a 文件。",
            "default": ""
          }
        }
      }
    }
  },
  "group_history": {
    "description": "群友史",
    "type": "object",
    "hint": "群友史聊天记录构造功能相关配置",
    "items": {
      "enable_group_history_feature": {
        "description": "是否启用群友史聊天记录构造功能。关闭后 /群友史 会提示功能已关闭，不再生成合并转发聊天记录。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      }
    }
  },
  "music_search": {
    "description": "音乐搜索与点歌",
    "type": "object",
    "hint": "网易云音乐 API 搜索、选择和语音播放相关配置",
    "items": {
      "enable_music_search_feature": {
        "description": "是否启用音乐搜索与点歌功能。关闭后 /点歌 不再搜索歌曲。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      },
      "api_base_url": {
        "description": "网易云音乐 API 地址。可填写完整地址，也可只填写 host:port。",
        "type": "string",
        "default": "http://64.90.12.120:3051",
        "hint": "http://64.90.12.120:3051"
      },
      "quality": {
        "description": "优先请求的音质。不可用时会自动尝试 exhigh、higher、standard。",
        "type": "string",
        "default": "exhigh",
        "hint": "lossless / exhigh / higher / standard"
      },
      "search_limit": {
        "description": "每次搜索展示的歌曲数量，范围 1-10。",
        "type": "int",
        "default": 5,
        "hint": "5"
      },
      "selection_timeout_seconds": {
        "description": "搜索后等待用户回复编号的秒数，范围 10-600。",
        "type": "int",
        "default": 60,
        "hint": "60"
      },
      "cookie": {
        "description": "可选的网易云 Cookie。需要访问受限歌曲时可配置，留空则不传。",
        "type": "text",
        "default": "",
        "hint": "MUSIC_U=..."
      }
    }
  },
  "offline_email_alert": {
    "description": "账号下线邮件通知",
    "type": "object",
    "hint": "检测到 AIOCQHTTP 账号下线通知时，通过 QQ 邮箱 SMTP 自动发送邮件。",
    "items": {
      "enable_offline_email_alert": {
        "description": "是否启用账号下线邮件通知。关闭后只检测事件，不发送邮件。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      },
      "sender": {
        "description": "QQ 邮箱发件人地址。",
        "type": "string",
        "default": "",
        "hint": "123456@qq.com"
      },
      "QQ_password": {
        "description": "QQ 邮箱 SMTP 授权码，不是 QQ 登录密码。",
        "type": "string",
        "default": "",
        "hint": "QQ 邮箱 SMTP 授权码"
      },
      "receiver": {
        "description": "告警邮件收件人地址。",
        "type": "string",
        "default": "",
        "hint": "receiver@example.com"
      }
    }
  },
  "history_chunking": {
    "description": "历史消息切割与读取工具",
    "type": "object",
    "hint": "历史消息切割与 read_current_history 工具相关配置",
    "items": {
      "enable_history_chunking_feature": {
        "description": "是否启用历史消息切割整套功能。关闭后不会切割 req.contexts，也不会让 read_current_history 工具返回历史片段。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      },
      "enable_history_message_chunking": {
        "description": "是否对超长历史消息进行分段切割。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      },
      "history_message_chunk_length": {
        "description": "启用切割后，每段历史消息的最大字符数。",
        "type": "int",
        "default": 100,
        "hint": "100"
      },
      "enable_history_read_tool": {
        "description": "是否向大模型暴露 read_current_history 历史读取工具。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      },
      "remove_history_read_tool_before_llm": {
        "description": "是否在请求大模型前从本轮工具集中移除 read_current_history。开启后会检测该工具是否存在，即使它未激活也会尝试按名称移除。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      },
      "history_read_tool_default_count": {
        "description": "历史读取工具默认每页读取的历史条数。",
        "type": "int",
        "default": 6,
        "hint": "6"
      }
    }
  },
  "stream_output": {
    "description": "输出流重整与发送方式",
    "type": "object",
    "hint": "逐句发送、随机间隔、合并转发打包发送相关配置",
    "items": {
      "enable_human_like_stream_delay": {
        "description": "是否在逐句发送时加入随机停顿，模拟真人聊天节奏。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      },
      "stream_delay_min_seconds": {
        "description": "逐句发送的最小随机停顿秒数。",
        "type": "float",
        "default": 0.35,
        "hint": "0.35"
      },
      "stream_delay_max_seconds": {
        "description": "逐句发送的最大随机停顿秒数。",
        "type": "float",
        "default": 1.2,
        "hint": "1.2"
      },
      "enable_forward_when_too_long": {
        "description": "是否在重整后的文本总字数达到阈值时使用合并转发打包发送。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      },
      "forward_text_length_threshold": {
        "description": "开启字数阈值打包发送后，重整后的文本总字数达到该值时改用合并转发。",
        "type": "int",
        "default": 200,
        "hint": "200"
      },
      "enable_forward_when_too_many_segments": {
        "description": "是否在重整后分段数量达到阈值时使用合并转发打包发送。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      },
      "forward_segment_count_threshold": {
        "description": "开启分段数量阈值打包发送后，重整出的分段数量达到该值时改用合并转发。",
        "type": "int",
        "default": 5,
        "hint": "5"
      }
    }
  },
  "debug_logging": {
    "description": "调试日志",
    "type": "object",
    "hint": "请求大模型前的输入与历史日志配置，可能包含隐私内容",
    "items": {
      "enable_llm_request_debug_log": {
        "description": "是否在 LLM 请求前打印 ProviderRequest 的调试元数据。日志可能包含隐私内容，建议仅调试时开启。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      },
      "enable_final_history_log": {
        "description": "是否在 LLM 请求前打印最终输入模型的完整历史。日志可能很长且包含隐私内容，建议仅调试时开启。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      },
      "enable_full_provider_request_log": {
        "description": "是否在 LLM 请求前打印 ProviderRequest 的全部公开属性快照。日志可能很长且包含隐私内容，建议仅调试时开启。",
        "type": "bool",
        "default": false,
        "hint": "true / false"
      }
    }
  },
  "li_handoff": {
    "description": "希罗交接",
    "type": "object",
    "hint": "let_li_speak 与下一轮希罗接管相关配置",
    "items": {
      "enable_let_li_speak_tool": {
        "description": "是否向 ni 的大模型暴露 let_li_speak 工具，并允许艾玛触发希罗原生对话流程。关闭后不会再向模型提供该工具，已存在的工具调用也会被拒绝。",
        "type": "bool",
        "default": true,
        "hint": "true / false"
      },
      "li_platform_id": {
        "description": "希罗在 AstrBot 中的平台 ID。留空则尝试自动匹配 'li'（忽略大小写）。",
        "type": "string",
        "default": "",
        "hint": "如 'li'、'Li'，留空自动匹配"
      }
    }
  }
}
